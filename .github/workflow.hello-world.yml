name: hello world
on: push
jobs:
  hello-world-jobs:
    runs-on: ubuntu-latest
    steps:
      -name: Check out repositorycode
      users: actions/checkout@v3
      -runs: echo "$(cat hello world.txt)"
